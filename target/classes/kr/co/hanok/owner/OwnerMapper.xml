<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.hanok.owner.OwnerMapper">

	<!--  사장님 회원가입  -->
		<insert id="insert" parameterType="kr.co.hanok.owner.OwnerVO"> 
			INSERT INTO USERS_OWNER (
				owner_id, owner_pwd, owner_tel, owner_email,owner_address, businessNum, owner_created
			) VALUES (
				#{owner_id},MD5(#{owner_pwd}), #{owner_tel}, #{owner_email}, #{owner_address}, #{owner_created}, #{businessNum},#{owner_created}
			) </insert>
			
	<!--  사장님 로그인  -->
		<select id="login" parameterType="map" resultType="kr.co.hanok.owner.OwnerVO"> 
			SELECT * FROM USERS_CUSTOMER WHERE owner_id=#{owner_id} AND owner_pwd = MD(#{owner_pwd}) </select>
</mapper>